FROM nginx:alpine
# Sitio estático: se copia tal cual. Sin paso de compilación a propósito —
# desplegar es apretar un botón, no arrastrar una cadena de herramientas.
COPY . /usr/share/nginx/html/
EXPOSE 80
